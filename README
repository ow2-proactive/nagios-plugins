To start the whole project you need to do:

1. Start the Scheduler/RM.
   cd ProActiveSchedulingResourcing-3.1.0_src
          (but make sure it is compiled everything doing:
	      cd compile
              ./build deploy.all 
          )
   ./bin/unix/scheduler-start
2. Start the Apache server (REST API).
   cd Projects/apache-tomcat-6.0.33
   ./bin/startup.sh -security
